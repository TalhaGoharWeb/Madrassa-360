# Madrassa-360 — environment template (Phase 2: multi-tenant SaaS)
# Copy to .env (repo root) or assets/.env and fill in real values.
# Never commit real keys.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Never commit real keys. Service-role key must NEVER be in the client — see docs/SECURITY_AUDIT.md
